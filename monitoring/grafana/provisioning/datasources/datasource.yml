# Grafana 데이터소스 프로비저닝
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    # compose 네트워크 안에서 DNS(prometheus) + 컨테이너 내부 포트 9090
    url: http://prometheus:9090
    isDefault: true
    editable: false
    jsonData:
      timeInterval: 15s