spring:
  application:
    name: product-service

  # 12-factor: DB 접속 정보는 코드에 박지 않고 전부 환경변수로 외부화
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3307/productdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul}
    username: ${SPRING_DATASOURCE_USERNAME:product}
    password: ${SPRING_DATASOURCE_PASSWORD:productpw}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  # Kafka: payment-events 구독(consumer) + product-events 발행(producer)
  # 재고 차감 트리거를 OrderCreated -> PaymentProcessed(APPROVED)로 변경
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9094}
    # 분산추적: Kafka 발행/수신에 observation을 켜 trace context를 메시지 헤더로 전파
    template:
      observation-enabled: true
    listener:
      observation-enabled: true
    # 결과 이벤트를 product-events로 발행
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: false
    consumer:
      group-id: product-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.use.type.headers: false
        spring.json.value.default.type: com.commerce.product.messaging.command.DeductStockCommand
        spring.json.trusted.packages: "*"

server:
  port: ${SERVER_PORT:8080}

management:
  # 분산추적: 모든 hop에 trace id 전파 -> Zipkin으로 전송
  tracing:
    sampling:
      probability: 1.0
  zipkin:
    tracing:
      endpoint: ${ZIPKIN_ENDPOINT:http://localhost:9411/api/v2/spans}
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always
      group:
        liveness:
          include: livenessState
        readiness:
          # product-db까지 준비돼야 readiness UP
          include: readinessState,db

logging:
  level:
    org.hibernate.SQL: debug